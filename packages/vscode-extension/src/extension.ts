// SPDX-FileCopyrightText: 2024 CalmStudio contributors - see NOTICE file
//
// SPDX-License-Identifier: Apache-2.0

import * as vscode from 'vscode';

/**
 * Called when the extension is activated.
 * Commands and listeners are registered here — see preview.ts for full implementation.
 */
export function activate(_context: vscode.ExtensionContext): void {
  // Implementation will be added in Task 3
}

/**
 * Called when the extension is deactivated.
 * VS Code handles cleanup via context.subscriptions automatically.
 */
export function deactivate(): void {
  // No-op — VS Code disposes subscriptions automatically
}
